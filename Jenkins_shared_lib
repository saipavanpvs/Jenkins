@Library("Jenkins-shared-library") _
pipeline {
    agent any
    stages {
        stage('shared library') {
            steps {
                script {
                    helloWorld()
                }
            }
        }
    }
}
