@Library("Jenkins-shared-library") _
pipeline {
    agent any
        stages {
            steps ('shared library') {
                script {
                    helloWorld()
                }
            }
        }
}